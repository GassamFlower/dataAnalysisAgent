# 数据分析 Agent 站点配置
# 所有请求统一走 Next.js（BFF 层），后端由 Next.js 内部转发

# HTTP 服务器（当前配置）
# 配置 HTTPS 后，此 server 块应改为 301 重定向到 HTTPS
server {
    listen 80;
    server_name _;  # 无域名，匹配所有请求

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    # HSTS（Strict-Transport-Security）仅在 HTTPS 下生效
    # 配置 HTTPS 后，取消下方注释以启用 HSTS
    # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # 认证端点（登录/注册/验证码）：更严格限流 1r/s，防密码爆破与验证码轰炸
    # 注意：正则 location 优先级高于前缀 location /api/，认证请求会命中此块
    location ~ ^/api/v1/auth/ {
        limit_req zone=login_limit burst=3 nodelay;

        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 认证接口无需 LLM 长调用，收紧超时
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # API 限流（在 Nginx 入口层限制）
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;

        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 超时配置（LLM 调用可能较慢）
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # 所有其他请求 → Next.js 前端
    location / {
        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 微信域名验证文件
    location ~ ^/MP_verify_.*\.txt$ {
        root /usr/share/nginx/html;
        access_log off;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 禁止访问敏感文件
    location ~* \.(env|git|htaccess)$ {
        deny all;
    }
}
