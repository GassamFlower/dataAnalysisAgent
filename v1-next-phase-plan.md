# V1 问卷预演平台 — 下阶段任务细化方案

> 生成时间：2026-08-06
> 基线：WORKLOG.md（M0~M7，50 次提交，截至 2026-07-20）
> 前提：已撤销 V2 电商垂直化转型，回归 V1 问卷预演平台单一产品线

---

## 一、本次清理结果确认

已删除全部 V2 电商相关内容（11 个 untracked 文件/目录 + 5 个被污染的 tracked 文件还原）。

**清理后工作区仅剩 4 项 V1 增强改动**（均为合法 V1 功能，保留）：

| 状态 | 文件 | 性质 |
|------|------|------|
| M | `web/app/(marketing)/page.tsx` | 仅行尾符变化，无实际改动 → 建议还原 |
| M | `web/app/api/report/[id]/export/route.ts` | 导出格式白名单新增 `ppt`（+1 行） |
| ?? | `web/app/api/questionnaire/wjx-import/` | 问卷星导入 BFF 转发（完整） |
| ?? | `web/app/api/report/[id]/polish/` | 报告润色 BFF（**空文件，半成品**） |

---

## 二、进行中功能收尾（P0 — 立即推进）

经全链路排查，4 个进行中功能的完成度如下：

### 任务 1：问卷星导入 — 补齐前端 UI 入口

**现状**：后端端点 + 解析服务 + 前端 BFF 均已就绪，**仅缺前端业务页面入口**。

| 层 | 文件 | 状态 |
|----|------|------|
| 后端端点 | `server/app/api/v1/questionnaire.py:479` `POST /wjx-import/{project_id}` | ✅ |
| 后端服务 | `server/app/services/wjx_parser.py`（Excel + Word 解析，247 行） | ✅ |
| 前端 BFF | `web/app/api/questionnaire/wjx-import/[id]/route.ts` | ✅ |
| 前端 UI | 问卷编辑页导入入口 | ❌ 缺失 |

**改动点**：
1. 在 `web/app/(app)/projects/[id]/questionnaire/page.tsx`（或对应问卷编辑页）新增「问卷星导入」按钮
2. 点击后弹出 Dialog：文件选择（.xlsx / .docx）→ 上传至 BFF → 展示解析预览（题目数、维度数、警告）→ 确认后写入题目表格
3. 复用现有题目表格组件渲染预览结果

**验收标准**：
- 上传问卷星导出的 Excel/Word，能正确解析出题目、选项、维度
- 解析失败时展示后端返回的 `warnings` 与错误明细
- 导入成功后题目表格自动刷新

**预估工时**：0.5 天

---

### 任务 2：报告润色（polish）— 打通全链路

**现状**：前端 UI 层已搭好，**BFF 转发为空文件、后端无润色端点**，属半成品。

| 层 | 文件 | 状态 |
|----|------|------|
| 前端组件 | `web/components/report/polish-button.tsx` | ✅ |
| 前端页面 | `web/app/(app)/projects/[id]/report/page.tsx`（已引用） | ✅ |
| 前端 hook | `web/lib/hooks/use-report.ts`（含 polish 方法） | ✅ |
| 前端 BFF | `web/app/api/report/[id]/polish/route.ts` | ❌ 空文件 |
| 后端端点 | 无 | ❌ |
| 后端服务 | 无 | ❌ |

**改动点**：
1. **后端**：在 `server/app/api/v1/report.py` 新增 `POST /{report_id}/polish`，接收报告段落，调用 LLM（复用现有 `llm_service` 多提供商 fallback）做学术化润色，返回润色后文本
2. **后端 Schema**：新增 `PolishRequest` / `PolishResponse`
3. **前端 BFF**：补全 `polish/route.ts`，转发到后端并透传鉴权头（参照 wjx-import 的写法）
4. **前端**：确认 `polish-button` 调用链路、loading 态、润色前后对比展示

**验收标准**：
- 点击「润色」按钮，报告指定段落经 LLM 润色后返回，学术语气提升
- 润色过程有 loading 反馈，失败有错误提示
- 润色计入配额（属 LLM 重操作，应纳入周配额）

**预估工时**：1 天

---

### 任务 3：PPT 导出 — 端到端验证

**现状**：后端服务 + 前端 BFF 放行均已就绪，需验证全链路闭环。

| 层 | 文件 | 状态 |
|----|------|------|
| 后端服务 | `server/app/services/ppt_exporter.py` | ✅ |
| 前端 BFF | `export/route.ts` 白名单含 `ppt` | ✅ |
| 后端端点 | export 是否分发到 ppt_exporter | ❓ 待验证 |
| 前端 UI | 导出按钮是否含 PPT 选项 | ❓ 待验证 |

**改动点**：
1. 核查后端 `report.py` 导出端点：`format == "ppt"` 时是否调用 `ppt_exporter`
2. 核查前端导出按钮组件：是否已暴露 PPT 选项
3. 实际跑一遍：生成报告 → 导出 PPT → 打开验证排版

**验收标准**：
- 报告页点击导出，可选 PPT 格式，下载文件可用 PowerPoint/WPS 正常打开
- PPT 内容与报告一致（封面、分析章节、图表）

**预估工时**：0.5 天（若链路已通则仅验证；若断链则补齐分发逻辑）

---

### 任务 4：营销首页 page.tsx — 还原

**现状**：仅行尾符（LF/CRLF）差异，无实际内容改动。

**改动点**：`git checkout -- "web/app/(marketing)/page.tsx"` 还原，保持工作区干净。

**预估工时**：即时

---

## 三、遗留风险闭环（P1 — 上线前必做）

源自 WORKLOG 第六节，共 5 项：

| # | 风险 | 处置方案 | 工时 |
|---|------|---------|------|
| 1 | 微信扫码登录待上线 | 待公众号资质到位后启用真实扫码链路；当前保留「待上线」提示。**外部依赖，暂挂** | — |
| 2 | dev-login 端点生产放开 | 上线前恢复 `DEBUG` 限制，或改为环境变量门控 + 操作审计日志 | 0.5 天 |
| 3 | PostgreSQL 5432 端口外放 | 收紧为仅内网/Docker 网络访问，或加 IP 白名单 + 强密码 + SSL | 0.5 天 |
| 4 | 免费配额 6 次/周 | 上线后监控 LLM 成本与活跃度，按数据调参（暂观察） | 持续 |
| 5 | demo.zip / error.txt 本地残留 | 加入 `.gitignore`，避免误提交 | 即时 |

---

## 四、平台增强与体验打磨（P2 — 按需推进）

V1 核心功能已 R3 验收，以下为锦上添花项，按用户反馈优先级排期：

| # | 增强项 | 价值 | 工时 |
|---|--------|------|------|
| A | **报告模板多样化** | 新增 2-3 套报告排版模板（简洁/学术/商业），用户可切换 | 2 天 |
| B | **问卷体检增强** | wjx 导入后自动跑题目体检（题量、量表方向、维度覆盖），给优化建议 | 1.5 天 |
| C | **批量项目管理** | 项目列表支持搜索/筛选/归档/标签，便于多项目用户管理 | 1.5 天 |
| D | **数据可视化看板** | 项目概览页增加图表（模拟进度、报告完成度、配额消耗趋势） | 2 天 |
| E | **导出格式扩充** | 报告支持 Markdown 导出，便于接入其他写作工具 | 0.5 天 |
| F | **移动端适配** | 核心页面（项目列表/报告查看）响应式优化 | 2 天 |

---

## 五、建议执行顺序与里程碑

```
本周（W1）  ┃ P0 收尾：任务4(还原) → 任务1(wjx UI) → 任务3(PPT验证) → 任务2(polish全链路)
            ┃ 产出：4 个进行中功能全部闭环，可形成一次提交
            ┃
下周（W2）  ┃ P1 风险：任务5(.gitignore) → 任务2(dev-login) → 任务3(PG安全)
            ┃ 产出：上线前安全清单清零
            ┃
W3 起       ┃ P2 增强：按用户反馈选 1-2 项推进（建议优先 B 问卷体检增强，与 wjx 导入形成闭环）
```

**里程碑建议**：
- **M8（W1 末）**：进行中功能收尾完成 → tag `v1.1.0`
- **M9（W2 末）**：上线前风险清零 → tag `v1.1.0-rc`
- **M10（W4）**：首批增强项交付 → tag `v1.2.0`

---

## 六、即时可执行的第一步

建议现在先做两件零成本的事，锁定进度：

1. **还原 page.tsx**（消除噪音改动）
2. **补 .gitignore**（demo.zip / error.txt）

随后进入任务 1（问卷星导入 UI），这是投入产出比最高的一项——后端已全部就绪，只需补前端入口即可上线一个完整功能。
