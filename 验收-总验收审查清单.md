# 上线前总验收 · 5 维度审查与整改清单

> 阶段：上线前总验收（本轮只审查 + 输出整改清单，不改代码）
> 审查范围：server/（FastAPI + SQLAlchemy + Alembic）、web/（Next.js BFF）、db/、deploy/、nginx/、scripts/、docker-compose.yml
> 规则基线：docs/j-Agent宪法.md、docs/数据库schema设计.md、docs/o-后端架构实施真源文档.md、docs/s-上线前验收报告.md
> 生成日期：2026-08（当前运行环境实际检查）

---

## 一、结构维度

| # | 判断 | 证据 | 说明 |
|---|------|------|------|
| S1 | 🟢 通过 | server/app 分层（api/v1、schemas、models、services、core、utils）职责清晰 | 与 `o-后端架构实施真源文档.md` 目录责任表基本一致 |
| S2 | 🟢 通过 | 统一响应 `code/message/data`、统一异常基类、统一认证依赖 | `core/responses.py`、`core/exceptions.py`、`core/dependencies.py` |
| S3 | 🟡 风险 | `app/utils/` 为空目录（仅 `__init__.py`） | 真源文档已标注"空目录占位"；建议清理或删成立即未用 |
| S4 | 🟡 风险 | `server/app/services/diff_test.py` 命名为测试文件，实为业务统计模块 | 命名易误导；若为 pytest 收集对象会被误收集（与踩坑 P6/P13 同类） |
| S5 | 🟡 风险 | `server/migrations/versions/` 提交了 `__pycache__/*.pyc`（含已删除迁移 `89331811bd94`、`a6b79e9f3748`、`8f3a2c1d9e7b` 的残留） | 仓库卫生：失效/幽灵迁移痕迹、双份 `diff_test.cpython-312-pytest-*.pyc` 需清理 |
| S6 | 🟡 风险 | `web/lib` 下 `auth.ts`（api）、`env.ts`、`backend-url.ts` 与 `lib/server/auth.ts` 存在职责近似 | BFF 内 `getAuthHeader` 与 `client.ts` 的 getAuthHeader 逻辑重复；建议收敛唯一来源 |

## 二、规则（宪法 / 真源 / 规范）

| 判断 | 证据 | 说明 |
|------|------|------|
| 🔴 严重 | `docs/j-Agent宪法.md` 第17条要求「双模型架构」：R1~R3=DeepSeek-V3(`deepseek-chat`)，R4=R1(`deepseek-reasoner)`，且**不可混用** | 实际代码 `server/.env*`/`app/services/llm/client.py:44-57`、`config.py:16-17` 使用的模型为 `deepseek-v4-flash` / `deepseek-v4-pro`（含 KIMI/QWEN 多供应商 fallback）。真源文档曾把此标为「已修复」，但修复目标仍是 v4 模型，**偏离宪法第17条**。需走「修宪」流程或在宪法同步锁定新模型 |
| 🔴 严重 | `宪法第10条`：问卷星链接解析/PDF导出/教程站列为「第二版（MVP 不做）」 | 以上均已实现并在用（`wjx_parser.py`、PDF 导出、`tutorial_articles`、PPT、SPSS、报告润色）。`PROJECT_STATUS` 定位已更新为 v2.0「预演+诊断」，但 `j-Agent宪法.md` 未相应改版——**章程未随实现修订**（违反第24/25条解释基准的修订流程） |
| 🟡 风险 | 状态机 status 与设计文件漂移 | `models/project.py:24` 约束 `draft/inspected/hypothesized/simulated/analyzed`，但 `数据库schema设计.md` 3.2 只写 `draft/inspected/simulated/reported`。违反第21-C/D 条「先改设计文件再改代码」 |
| 🟡 风险 | users 表结构漂移 | 设计文件 users 表无 `email/email_verified/password_hash/is_admin/refresh_token/email_verify_code_hash`，模型与迁移均已加。设计真源未回写 |
| 🟢 通过 | 第7条「水印铁律」、第4/5/6/8条学术伦理 | `services/reporter.py`（Word/Excel/CSV/PDF docx PDf）与 `ppt_exporter.py` 强制 `simulated` 水印 + 免责声明 + 文件名带 `simulated`，未发现"去水印"路径。第6条暴力调数据未发现 |
| 🟡 风险 | `PROJECT_STATUS.md` 门禁「已上线」与本文档审查矛盾的历史（v1.4 多次新增模块未拆「是否仍属 MVP」） | 属文档口径与宪章修订不同步的表现 |

---

## 三、安全（侧重后端）

|  # | 判断 | 证据 | 说明 |
|----|------|------|------|
| 1 | 🔴 严重 | `server/.env.production`：明文 API Key（DeepSeek `sk-56c…`、`AGNES_API_KEY=sk-4c…`、SMTP 授权码、微信 AppSecret `wxa…`）| 该文件被 `.gitignore` 忽略，但**已落到运行机器上且含生产资料**；`RESET_JWT_SECRET_KEY=change_to_another_strong_random_secret_for_password_reset` 仍是占位串（`_validate_production_settings` 只查长度≠JWT、不查是否为占位符）→ 生产以弱/占位密钥跑 |
| 2 | 🔴 | `FRONTEND_URL=http://<server-ip>`（明文 HTTP）+ nginx 仅 `listen 80`无 TLS | 生产无 HTTPS：JWT/登录密码/验证码/支付回调全部明文传输；`nginx/conf.d/default.conf` 注释也写明「配置 HTTPS 后置 301」，实为未配置。上线阻断项 |
| 3 | 🔴 | 支付回调 IP 白名单实为关闭：`api/v1/payment.py:128-156` + `.env.production.example` 未设 `PAYMENT_CALLBACK_TOKEN` / `PAYMENT_ALLOWED_IPS` | 生产下 `/payment/orders/{id}/notify` 只靠「静态 token 等值比对」header 防护（非 constant-time）。默认 token 为空则回调直接 403：支付功能在参考配置下不可用；配上有值则仅存较弱防护 |
| 4 | 🟡 | 未做越权（IDOR）漏洞 | 抽查所有按 project_id 资源路由（projects/simulation/report/dataset/questionnaire）统一经 `services/project_service.py:47-67` 的 `get_owned_project(db, project_id, current_user["id"])`；订单 user_id 隔离；未发现跨用户越权 |
| 5 | 🟡 | SQL 注入：通过 | `monitoring.py:37` 唯一 `select(text("SELECT 1"))` 为静态健康检查；其余全为参数化 ORM |
| 6 | 🟡 | 输入边界不足（可用性）| `schemas/simulation.py:50-54` sample_size 仅 `gt=0`（无上限 → 内存 DoS）；`schemas/simulation.py:96-109` 矩阵 `value` 未限 `[-1,1]`、shape 未校验 dim×dim 方阵 → `generator.py` 的 `multivariate_normal` 可能非 PSD 抛 500 |
| 7 | 🟡 | 配额在归属校验前扣减 | `report.py:394→403`、`simulation.py` 先 `check_and_consume_quota` 后 `get_owned_project`，本地用户会被他人资源浪费额度（tutorial 顺序正确，应统一） |
| 8 | 🔴 | 功能 Bug：问卷质量体检 `/questionnaire/{id}/health` 恒 500 | `api/v1/questionnaire.py:570` 用 `user=Depends(get_current_user)`（dict）后取 `user.id` → `AttributeError` → 500。该接口不可用（v1.2 体检核心能力） |

### 过度防御 / 可化归
- 🟢 `main.py:87` CORS `allow_origins` 三元两分支完全一致，死分支。
- 🟢 `questionnaire.py:38-49` 所有扩展都放行 `application/octet-stream`，MIME 白名单实质失效，真正的防线是魔数校验（`_FILE_MAGIC_BYTES`）。
- ✅ 优点：文件上传魔数+MIME+大小限制、埋点 user_id 服务端派生、LLM 配置管理白名单、payback 金额服务端定价。

---

## 4. 数据 / 结构数据

|  # | 判断 | 证据 | 说明 |
|----|------|------|------|
| 1 | 🟢 通过 | 范式 | 主要表符合三范式；预计算结果（reports.overall_alpha 等）均已在设计文件"反范式"章节声明同步策略 |
| 2 | 🟡 | 迁移链无分叉 but 需确认 | 10 个迁移脚本组成单链：`529298d→…→a1b2c3d4e5f6`；`down_revision` 命名对象一致。但 `versions/__pycache__` 含多个已删迁移的 .pyc 幽灵，建议清理并复核 `alembic upgrade head` 可干净执行 |
| 3 | 🟡 | 敏感数据 | 密码 bcrypt✓、验证码 bcrypt 哈希✓、refresh token SHA256 哈希✓。但 `audit_logs.action_detail` / `analytics_events.metadata_json` / `user_agreements` 会持久化 `ip_address + user_agent`，属合规需要但建议明确保留周期；`llm_configs.config_value` 明文存 API Key（管理员域） |
| 4 | 🟢 | 软删除 | `projects` 用 `deleted_at` 软删且 `get_owned_project` 统一过滤软删，合规表（user_agreements/audit_logs）刻意不软删有设计注释 |
| 5 | 🔴 | `scripts/migrate_dev_to_prod.py`：迁移脚本蜕化/Destructive | users REDO `DELETE FROM users` 再重插，且只迁移 12 张基础表（缺 user_quotas/llm_configs/user_agreements/audit_logs/tutorial*/analtics 等）；users INSERT 未含 email/password_hash/refresh_token/is_admin → 迁移后账号不可用。生产迁移工具不可直接用 |
| 6 | 🟢 | 索引 | FK 列均有索引（idx_*_user_id 等）、user_quotas 有 UNIQUE(user_id,action_type,period_key)、datasets 1:1 上唯一索引 |

---

## 5. 可维护性

|  # | 判断 | 证据 | 说明 |
|----|------|------|------|
| 1 | 🔴 | 本地运行环境不一致 / 测试无法跑 | 本地 `python` 与 `server/venv` 均为 **Python 3.8.6**，但代码大量用 `dict[str,...]`、`X | None`(3.9/3.10+) 语法；`pytest` 直接在 `auth.py:53` `dict[str,tuple]` 处 `TypeError` → 测试套件无法在本机执行。而 Dockerfile 为 `python:3.11-slim`。真源文档已标注「本地 3.8 vs Dockerfile 3.11」，但现状仍未统一 → 验收可复现性断裂。触发宪法第 22 章「Python 3.8 断裂即升级 3.11/3.12」 |
| 2 | 🟡 | 统计阈值未完全集中 | 需要确认 `services/stats.py`/`diagnoser.py` 是否仍散写 0.7/0.6/0.8 等（应以 `core/statistics_constants.py` 为唯一真源）。若存在→违反「数值/阈值集中」规则 |
| 3 | 🟡 | 命名不一致 | `questionnaire_health.py` / `sample_representativeness.py` / `sample_size_planner.py` / `report_polisher.py` 命名风格不统一；`diff_test.py` 是业务模块（见 S4） |
| 4 | 🟢 | 重复实现 | `get_owned_project` 已收敛为公共函数（好)；但 multiple 路由重复装配分页列表构造；`getAuthHeader` 在前端两处重复 |
| 5 | 🟡 | 测试覆盖空眼 | `tests/` 有 17 个测试文件，覆盖面不错，但对 `payment callback`、`questionnaire health`、`matrix value`、`sample_size` 等新边界缺负向用例；健康端点 Bug(安全#8)漏网，说明缺少对 `<error_route> 500` 的回归 |

---

## 整改清单（按优先级排序）

### 🔴 阻断上线（必须修或必须决策）
1. HTTPS：nginx 配置 443/证书 + HSTS；把 `FRONTEND_URL` 与生产回调改为 `https://`。（nginx/conf.d、deploy/、server/.env.production）
2. secrets 管理：生产 key 移出 `.env.production` 改密注入/密钥管理；清掉占位 `RESET_JWT_SECRET_KEY`；确认 `KIMI_API_KEY`/`QWEN_API_KEY` 不再留占位符；确认 `debug=false`、`ALLOW_DEV_TOKEN=false`。
3. 修 `questionnaire.py:570` `user.id` → `user["id"]`（体检 500）。
4. 支付回调：生产示例补 `PAYMENT_CALLBACK_TOKEN` + `PAYMENT_ALLOWED_IPS`，确认签名/**恒时比较**；把 IP 白名单设为必配。
5. 宪法同步：对第七条/第10条/第17条的偏离提交「修宪」评审；未修宪前明确允许上线边界。
6. Python 环境统一：本地与 venv 升到 3.11/3.12，跑通全量测试（当前本机不可复现 181 passed）。

### 🟡 可上线后修（但建议本轮一起）
7. `schemas/simulation.py`：sample_size 加上限（如 ≤2000/10000）；`MatrixSaveCell.value` 限 [-1,1]、校验方阵、对角线=1、对称；generator 加入 `square=True` / PSD fallback。
8. 额度配额在归属校验之后才扣（report/simulation 统一顺序）。
9. `scripts/migrate_dev_to_prod.py`：改为幂等/UTF-8 全表，补全表与字段（email/password_hash/refresh_token/is_admin），加数据校验；否则禁用。
10. 清理 `migrations/versions/__pycache__/*.pyc` 幽灵；移空 `app/utils`；重命名 `diff_test.py`。
11. 校验 `statistics_constants` 唯一真源（硬编码阈值清零）。
12. `reset_password` 的提交语义：虽然 `get_db` 会 commit，但建议显式 `await db.commit()` 以免依赖隐式提交。
13. 补充测试：问卷 health、矩阵非法值、批量 sample_size、pay callback 负向。

### 🟢 建议（可选）
- 统一 CORS 分支（main.py:87）、前端 `getAuthHeader` 去重、BFF X-Forwarded-For 透传以恢复 nginx 单入口速率限可靠。
- 补充运维加固项：nginx.d / deploy 文档更新为 HTTPS；fail2ban 保留。

> 本轮仅审查，未改动任何项目代码。