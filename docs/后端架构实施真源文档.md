# 后端架构实施真源文档

> **文档性质**：基于实际代码验收结果反推，反映真实实现而非设计意图  
> **生成时间**：2026-07-13  
> **证据来源**：`server/app/` 目录下所有源码文件（含行号引用）

---

## 1. 语言框架（实际版本）

| 组件 | 实际版本约束 | 证据位置 |
|------|------------|---------|
| **Python** | >=3.10（推断自 `uuid.UUID` 类型注解 + `match` 语法未使用） | `server/app/models/user.py:18` 使用 `Mapped[uuid.UUID]` |
| **FastAPI** | >=0.110.0 | `server/requirements.txt:2` |
| **Uvicorn** | >=0.27.0（standard 扩展） | `server/requirements.txt:3` |
| **SQLAlchemy** | >=2.0.0（asyncio 扩展） | `server/requirements.txt:12` |
| **Pydantic** | >=2.5.0 | `server/requirements.txt:7` |
| **Pydantic-Settings** | >=2.1.0 | `server/requirements.txt:8` |
| **PyJWT** | >=2.8.0 | `server/requirements.txt:32` |
| **passlib** | >=1.7.4（bcrypt 扩展） | `server/requirements.txt:33` |
| **slowapi** | >=0.1.9 | `server/requirements.txt:34` |
| **aiosqlite** | 通过 `sqlite+aiosqlite://` URL 推断 | `server/.env:18` |

**未明确**：Python 精确版本（无 `.python-version` 或 `pyproject.toml` 文件）

---

## 2. 目录责任表（实际职责 + 文件路径）

```
server/app/
├── main.py                    # 应用入口：FastAPI 实例、生命周期、中间件、异常处理器
│                              # 证据：server/app/main.py:40-112
│
├── core/                      # 核心基础设施
│   ├── config.py              # 配置管理：从 .env 加载，定义 Settings 类
│   │                          # 证据：server/app/core/config.py:9-44
│   ├── database.py            # 数据库连接：异步引擎、会话工厂、WAL 模式
│   │                          # 证据：server/app/core/database.py:21-50
│   ├── dependencies.py        # 依赖注入：认证（get_current_user）、权限（require_paid_plan）
│   │                          # 证据：server/app/core/dependencies.py:22-93
│   ├── exceptions.py          # 自定义异常：AppException 基类 + 5 个子类
│   │                          # 证据：server/app/core/exceptions.py:5-60
│   ├── middleware.py          # 中间件：速率限制（limiter）、请求日志（RequestLoggingMiddleware）
│   │                          # 证据：server/app/core/middleware.py:30-68
│   ├── responses.py           # 统一响应模型：ResponseModel、ErrorResponse、error_response()
│   │                          # 证据：server/app/core/responses.py:9-31
│   ├── security.py            # 安全工具：JWT 生成/验证、密码哈希
│   │                          # 证据：server/app/core/security.py:15-91
│   ├── logging.py             # 日志配置：格式、控制台/文件输出、第三方库级别抑制
│   │                          # 证据：server/app/core/logging.py:9-38
│   └── statistics_constants.py # 统计常量：信效度分档表、通过阈值、分档函数
│                              # 证据：server/app/core/statistics_constants.py:1-182
│
├── api/v1/                    # 路由层（API 端点）
│   ├── __init__.py            # 路由聚合：注册 auth/projects/questionnaire/simulation/report
│   │                          # 证据：server/app/api/v1/__init__.py:6-11
│   ├── auth.py                # 认证模块：dev-login（测试账号登录，返回 JWT，不限 DEBUG）
│   │                          # 证据：server/app/api/v1/auth.py:17-46
│   ├── projects.py            # 项目 CRUD：列表/创建/详情/更新/删除
│   │                          # 证据：server/app/api/v1/projects.py:24-166
│   ├── questionnaire.py       # 问卷模块：上传/体检/题目管理
│   ├── simulation.py          # 模拟模块：假设解析/数据生成/信效度检验/差异检验
│   └── report.py              # 报告模块：报告生成/导出
│
├── models/                    # ORM 模型（SQLAlchemy 2.0 声明式映射）
│   ├── __init__.py            # 模型聚合：Base 基类 + 13 个模型导入
│   │                          # 证据：server/app/models/__init__.py:5-40
│   ├── user.py                # 用户模型：id/openid/nickname/plan/plan_expires_at
│   │                          # 证据：server/app/models/user.py:13-35
│   ├── project.py             # 项目模型：id/user_id/name/status + 7 个关系
│   │                          # 证据：server/app/models/project.py:13-48
│   ├── question.py            # 题目模型
│   ├── hypothesis.py          # 假设模型
│   ├── hypothesis_path.py     # 假设路径模型
│   ├── correlation_matrix.py  # 相关矩阵模型
│   ├── simulation_config.py   # 模拟配置模型
│   ├── dataset.py             # 数据集模型
│   ├── report.py              # 报告模型
│   ├── reliability_result.py  # 信度结果模型
│   ├── diagnosis.py           # 诊断模型
│   ├── diagnosis_issue.py     # 诊断问题模型
│   └── order.py               # 订单模型
│
├── schemas/                   # Pydantic 请求/响应模型
│   ├── common.py              # 通用模型：ResponseModel/ErrorResponse/PaginatedData
│   │                          # 证据：server/app/schemas/common.py:12-41
│   ├── project.py             # 项目相关 schema
│   ├── questionnaire.py       # 问卷相关 schema
│   ├── simulation.py          # 模拟相关 schema
│   ├── report.py              # 报告相关 schema
│   └── user.py                # 用户相关 schema
│
└── services/                  # 业务逻辑层
    ├── llm/client.py          # LLM 客户端：DeepSeek API 调用
    ├── diagnoser.py           # 诊断器：规则匹配 + LLM 补充
    ├── diagnosis_rules.py     # 诊断规则：硬伤判定逻辑
    ├── generator.py           # 数据生成器：模拟数据生成
    ├── hypothesis_parser.py   # 假设解析器：自然语言→结构化假设
    ├── inspector.py           # 体检器：题目质量检查
    ├── reporter.py            # 报告生成器：Word/Excel 导出
    ├── stats.py               # 统计计算：信效度检验、差异检验
    └── diff_test.py           # 差异检验：t 检验/方差分析
```

---

## 3. 接口响应规则（实际格式 + 示例文件）

### 3.1 成功响应格式

**实际格式**：
```json
{
  "code": 0,
  "message": "success",
  "data": <T>
}
```

**证据位置**：`server/app/core/responses.py:9-14`
```python
class ResponseModel(BaseModel, Generic[T]):
    """成功响应模型。"""
    code: int = 0
    message: str = "success"
    data: Optional[T] = None
```

**实际使用示例**：`server/app/api/v1/projects.py:60`
```python
return ResponseModel(data=data)
```

### 3.2 错误响应格式

**实际格式**：
```json
{
  "code": <int>,
  "message": "<str>",
  "details": <dict>
}
```

**证据位置**：`server/app/core/responses.py:17-22`
```python
class ErrorResponse(BaseModel):
    """错误响应模型。"""
    code: int
    message: str
    details: Optional[dict] = None
```

**构建函数**：`server/app/core/responses.py:30-31`
```python
def error_response(code: int, message: str, details: Optional[dict] = None) -> dict:
    """构建错误响应。"""
    return {"code": code, "message": message, "details": details or {}}
```

### 3.3 分页数据格式

**实际格式**：
```json
{
  "items": [<T>],
  "total": <int>,
  "page": <int>,
  "page_size": <int>
}
```

**证据位置**：`server/app/schemas/common.py:35-41`
```python
class PaginatedData(BaseModel, Generic[T]):
    """分页数据。"""
    items: List[T]
    total: int
    page: int
    page_size: int
```

**实际使用示例**：`server/app/api/v1/projects.py:54-60`
```python
data = PaginatedData(
    items=projects,
    total=total,
    page=page,
    page_size=page_size
)
return ResponseModel(data=data)
```

---

## 4. 错误处理（实际位置 + 代码示例）

### 4.1 自定义异常体系

**基类**：`server/app/core/exceptions.py:5-19`
```python
class AppException(Exception):
    """应用基础异常。"""
    def __init__(
        self,
        code: int,
        message: str,
        details: Optional[dict] = None,
        status_code: int = 400,
    ):
        self.code = code
        self.message = message
        self.details = details or {}
        self.status_code = status_code
        super().__init__(message)
```

**子类清单**：

| 异常类 | code 范围 | HTTP 状态码 | 证据位置 |
|-------|----------|------------|---------|
| `NotFoundException` | 40400 | 404 | `server/app/core/exceptions.py:22-26` |
| `ValidationException` | 40000 | 400 | `server/app/core/exceptions.py:29-33` |
| `UnauthorizedException` | 40100 | 401 | `server/app/core/exceptions.py:36-40` |
| `ForbiddenException` | 40300 | 403 | `server/app/core/exceptions.py:43-47` |
| `BusinessException` | 60000-69999 | 400（可自定义） | `server/app/core/exceptions.py:50-60` |

### 4.2 全局异常处理器

**自定义异常处理器**：`server/app/main.py:69-76`
```python
@app.exception_handler(AppException)
async def app_exception_handler(request: Request, exc: AppException):
    """处理自定义异常。"""
    logger.warning(f"业务异常: {exc.code} - {exc.message}")
    return JSONResponse(
        status_code=exc.status_code,
        content=error_response(exc.code, exc.message, exc.details),
    )
```

**Pydantic 校验异常处理器**：`server/app/main.py:79-92`
```python
@app.exception_handler(RequestValidationError)
async def validation_exception_handler(request: Request, exc: RequestValidationError):
    """处理 Pydantic 请求体校验异常，统一为 ErrorResponse 格式。"""
    errors = exc.errors()
    first = errors[0] if errors else {}
    loc = " -> ".join(str(l) for l in first.get("loc", []))
    msg = first.get("msg", "参数校验失败")
    detail = f"{loc}: {msg}" if loc else msg
    logger.info(f"参数校验失败: {detail}")
    return JSONResponse(
        status_code=422,
        content=error_response(42200, detail, {"errors": errors}),
    )
```

**未捕获异常处理器**：`server/app/main.py:95-102`
```python
@app.exception_handler(Exception)
async def general_exception_handler(request: Request, exc: Exception):
    """处理未捕获异常。"""
    logger.error(f"未捕获异常: {exc}", exc_info=True)
    return JSONResponse(
        status_code=500,
        content=error_response(50000, "服务器内部错误"),
    )
```

### 4.3 业务异常使用示例

**资源未找到**：`server/app/api/v1/projects.py:107`
```python
raise NotFoundException("项目不存在")
```

**未授权**：`server/app/core/dependencies.py:33`
```python
raise UnauthorizedException("未提供认证凭据")
```

**禁止访问（权限不足）**：`server/app/core/dependencies.py:90`
```python
raise ForbiddenException("该功能需要付费套餐（单次解锁或订阅）")
```

---

## 5. 日志（实际配置 + 输出示例）

### 5.1 日志配置

**配置文件**：`server/app/core/logging.py:9-38`

**日志格式**：
```
%(asctime)s | %(levelname)-8s | %(name)s:%(lineno)d | %(message)s
```

**日期格式**：`%Y-%m-%d %H:%M:%S`

**输出目标**：
- **开发环境（DEBUG=True）**：仅控制台（stdout）
- **生产环境（DEBUG=False）**：控制台 + 文件（`app.log`，10MB 轮转，保留 5 份）

**第三方库抑制**：
```python
logging.getLogger("uvicorn.access").setLevel(logging.WARNING)
logging.getLogger("sqlalchemy.engine").setLevel(logging.WARNING)
```

证据：`server/app/core/logging.py:33-34`

### 5.2 请求日志中间件

**中间件实现**：`server/app/core/middleware.py:39-68`

**日志格式**：
```
[YYYY-MM-DD HH:MM:SS] METHOD /path STATUS_CODE TIMEs
```

**实际输出示例**（从服务日志提取）：
```
[2026-07-13 08:09:04] GET /health 200 0.001s
[2026-07-13 08:09:05] GET /api/v1/projects/ 200 0.045s
[2026-07-13 08:09:06] POST /api/v1/questionnaire/upload 401 0.002s
```

**日志级别选择**：
- `status_code >= 500` → ERROR
- `status_code >= 400` → WARNING
- 其他 → INFO

证据：`server/app/core/middleware.py:59-67`

### 5.3 业务日志示例

**启动日志**：`server/app/main.py:30-32`
```python
logger.info("启动数据分析智能体 API...")
await init_db()
logger.info("数据库初始化完成")
```

**异常日志**：
- 业务异常：`logger.warning(f"业务异常: {exc.code} - {exc.message}")`（`server/app/main.py:72`）
- 参数校验：`logger.info(f"参数校验失败: {detail}")`（`server/app/main.py:88`）
- 未捕获异常：`logger.error(f"未捕获异常: {exc}", exc_info=True)`（`server/app/main.py:98`）

---

## 6. 数据库连接（实际配置 + 文件）

### 6.1 数据库引擎配置

**配置文件**：`server/app/core/database.py:9-21`

**实际配置**：
```python
engine_kwargs = {
    "echo": settings.DEBUG,  # 开发环境打印 SQL
}

if settings.DATABASE_URL.startswith("sqlite"):
    engine_kwargs["connect_args"] = {"check_same_thread": False}
else:
    engine_kwargs["pool_size"] = 20
    engine_kwargs["max_overflow"] = 10
    engine_kwargs["pool_pre_ping"] = True

engine = create_async_engine(settings.DATABASE_URL, **engine_kwargs)
```

**当前环境**：SQLite + aiosqlite  
**证据**：`server/.env:18`
```
DATABASE_URL=sqlite+aiosqlite:///./data_analysis_agent.db
```

### 6.2 SQLite 性能优化

**WAL 模式 + busy_timeout**：`server/app/core/database.py:26-32`
```python
if settings.DATABASE_URL.startswith("sqlite"):
    @event.listens_for(engine.sync_engine, "connect")
    def _set_sqlite_pragma(dbapi_connection, connection_record):
        cursor = dbapi_connection.cursor()
        cursor.execute("PRAGMA journal_mode=WAL")
        cursor.execute("PRAGMA busy_timeout=5000")
        cursor.close()
```

**作用**：
- WAL（Write-Ahead Logging）：允许并发读写，避免 "database is locked" 错误
- busy_timeout=5000：写锁等待超时 5 秒

### 6.3 会话工厂

**配置**：`server/app/core/database.py:35-37`
```python
async_session = sessionmaker(
    engine, class_=AsyncSession, expire_on_commit=False
)
```

**关键参数**：
- `class_=AsyncSession`：使用异步会话
- `expire_on_commit=False`：提交后不失效对象，避免延迟加载问题

### 6.4 依赖注入

**会话生成器**：`server/app/core/database.py:40-50`
```python
async def get_db():
    """获取数据库会话（依赖注入）。"""
    async with async_session() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
        finally:
            await session.close()
```

**使用方式**：路由函数通过 `db: AsyncSession = Depends(get_db)` 注入

**证据**：`server/app/api/v1/projects.py:33`

### 6.5 数据库初始化

**启动时创建表**：`server/app/core/database.py:53-58`
```python
async def init_db():
    """初始化数据库（创建表）。"""
    from app.models import Base
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
```

**调用位置**：`server/app/main.py:31`（lifespan 启动阶段）

---

## 7. 权限入口（实际中间件 + 文件路径）

### 7.1 认证依赖

**核心函数**：`server/app/core/dependencies.py:22-81`

**支持的认证方式**：
1. **JWT token**（生产环境，通过 `/api/v1/auth/dev-login` 获取）
2. **dev-token**（仅 DEBUG=True 且 ALLOW_DEV_TOKEN=True，开发调试用）

**认证流程**：
```python
async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: AsyncSession = Depends(get_db),
) -> dict:
    if not credentials:
        raise UnauthorizedException("未提供认证凭据")

    token = credentials.credentials

    # 开发模式：允许 dev-token
    if settings.DEBUG and settings.ALLOW_DEV_TOKEN and token == settings.DEV_TOKEN:
        # 返回或创建 dev 用户
        ...

    # JWT 验证
    payload = verify_token(token)
    if not payload:
        raise UnauthorizedException("无效的认证凭据")

    # 查询用户
    user = await db.get(User, user_id)
    if not user:
        raise UnauthorizedException("用户不存在")

    return {
        "id": user.id,
        "nickname": user.nickname,
        "is_admin": False,
        "plan": user.plan,
        "plan_expires_at": user.plan_expires_at,
    }
```

**JWT 获取方式**：
- 前端调用 BFF `/api/auth/login` → BFF 转发到后端 `/api/v1/auth/dev-login` → 返回 JWT
- 后端 auth 端点：`server/app/api/v1/auth.py:17-46`（测试账号登录，不限 DEBUG）
- BFF 路由：`web/app/api/auth/login/route.ts`

**dev-token 配置**：`server/.env:26-27`
```
DEV_TOKEN=dev-token
ALLOW_DEV_TOKEN=true
```

### 7.2 权限检查依赖

**付费套餐检查**：`server/app/core/dependencies.py:84-93`
```python
async def require_paid_plan(user: dict = Depends(get_current_user)) -> dict:
    """要求付费套餐（single / subscription）。"""
    if user["plan"] == "free":
        raise ForbiddenException("该功能需要付费套餐（单次解锁或订阅）")
    if user["plan_expires_at"] and user["plan_expires_at"] < datetime.utcnow():
        raise ForbiddenException("套餐已过期，请续费")
    return user
```

**使用方式**：付费端点通过 `user: dict = Depends(require_paid_plan)` 注入

### 7.3 数据归属校验

**实际实现**：路由层通过 `current_user["id"]` 过滤数据

**示例**：`server/app/api/v1/projects.py:39-48`
```python
# 查询当前用户的项目
count_result = await db.execute(
    select(Project).where(Project.user_id == current_user["id"])
)

result = await db.execute(
    select(Project)
    .where(Project.user_id == current_user["id"])
    .order_by(Project.created_at.desc())
    ...
)
```

**未明确**：是否有统一的归属校验中间件（当前在路由层手动过滤）

### 7.4 HTTPBearer 配置

**配置**：`server/app/core/dependencies.py:16`
```python
security = HTTPBearer(auto_error=False)
```

**作用**：`auto_error=False` 允许手动处理未提供认证凭据的情况（返回 401 而非 403）

---

## 8. 框架复用边界（实际用了哪些框架能力）

### 8.1 FastAPI 能力复用

| 能力 | 实际使用 | 证据位置 |
|-----|---------|---------|
| **路由系统** | `APIRouter` + `@router.get/post/patch/delete` | `server/app/api/v1/projects.py:24-166` |
| **依赖注入** | `Depends(get_db)`, `Depends(get_current_user)` | `server/app/api/v1/projects.py:33-34` |
| **异常处理** | `@app.exception_handler` | `server/app/main.py:69-102` |
| **中间件** | `app.add_middleware(CORSMiddleware/SlowAPIMiddleware/RequestLoggingMiddleware)` | `server/app/main.py:53-65` |
| **Pydantic 模型** | `response_model=ResponseModel[...]` | `server/app/api/v1/projects.py:26` |
| **请求校验** | `RequestValidationError` 自动触发 | `server/app/main.py:79-92` |
| **生命周期** | `lifespan` 异步上下文管理器 | `server/app/main.py:26-37` |

### 8.2 SQLAlchemy 2.0 能力复用

| 能力 | 实际使用 | 证据位置 |
|-----|---------|---------|
| **异步 ORM** | `AsyncSession`, `create_async_engine` | `server/app/core/database.py:21-37` |
| **声明式映射** | `DeclarativeBase`, `Mapped`, `mapped_column` | `server/app/models/user.py:5-35` |
| **关系定义** | `relationship(back_populates=...)` | `server/app/models/user.py:34-35` |
| **查询构建** | `select()`, `where()`, `order_by()`, `offset()`, `limit()` | `server/app/api/v1/projects.py:38-51` |
| **事件监听** | `@event.listens_for(engine.sync_engine, "connect")` | `server/app/core/database.py:27-32` |

### 8.3 Pydantic 能力复用

| 能力 | 实际使用 | 证据位置 |
|-----|---------|---------|
| **请求模型** | `ProjectCreate`, `ProjectUpdate` | `server/app/api/v1/projects.py:71, 119` |
| **响应模型** | `ProjectResponse`, `ProjectListResponse` | `server/app/api/v1/projects.py:26, 65` |
| **泛型模型** | `ResponseModel[T]`, `PaginatedData[T]` | `server/app/core/responses.py:9-14`, `server/app/schemas/common.py:35-41` |
| **配置管理** | `BaseSettings` + `.env` 加载 | `server/app/core/config.py:9-44` |

### 8.4 第三方库复用

| 库 | 实际用途 | 证据位置 |
|----|---------|---------|
| **PyJWT** | JWT 生成/验证 | `server/app/core/security.py:6, 38-66` |
| **passlib** | 密码哈希（bcrypt） | `server/app/core/security.py:7, 12, 69-91` |
| **slowapi** | 速率限制 | `server/app/core/middleware.py:7-8, 30-33` |
| **aiosqlite** | SQLite 异步驱动 | `server/.env:18` |
| **openai** | DeepSeek API 调用（兼容 OpenAI SDK） | `server/requirements.txt:17` |

**未明确**：`openai` 库的具体使用位置（可能在 `services/llm/client.py`，但未详细读取）

---

## 9. 新增模块规则（实际要改哪些地方）

### 9.1 新增路由模块

**步骤**：
1. 在 `server/app/api/v1/` 下创建新文件（如 `new_module.py`）
2. 定义 `router = APIRouter(prefix="/new_module", tags=["new_module"])`
3. 在 `server/app/api/v1/__init__.py` 中导入并注册：
   ```python
   from app.api.v1 import new_module
   router.include_router(new_module.router)
   ```

**证据**：`server/app/api/v1/__init__.py:4-10`

### 9.2 新增 ORM 模型

**步骤**：
1. 在 `server/app/models/` 下创建新文件（如 `new_model.py`）
2. 定义模型类继承 `Base`
3. 在 `server/app/models/__init__.py` 中导入：
   ```python
   from app.models.new_model import NewModel
   ```
4. 添加到 `__all__` 列表

**证据**：`server/app/models/__init__.py:11-40`

### 9.3 新增 Pydantic Schema

**步骤**：
1. 在 `server/app/schemas/` 下创建或修改文件
2. 定义请求/响应模型
3. 在路由文件中导入使用

**证据**：`server/app/schemas/project.py`（未详细读取，但被 `projects.py` 导入）

### 9.4 新增业务服务

**步骤**：
1. 在 `server/app/services/` 下创建新文件
2. 实现业务逻辑函数
3. 在路由文件中导入调用

**证据**：`server/app/services/` 目录下已有 `diagnoser.py`, `generator.py`, `inspector.py`, `reporter.py`, `stats.py` 等

### 9.5 新增依赖注入

**步骤**：
1. 在 `server/app/core/dependencies.py` 中定义新依赖函数
2. 在路由函数签名中通过 `Depends()` 注入

**证据**：`server/app/core/dependencies.py:22-93`（`get_current_user`, `require_paid_plan`）

### 9.6 新增中间件

**步骤**：
1. 在 `server/app/core/middleware.py` 中定义中间件类（继承 `BaseHTTPMiddleware`）
2. 在 `server/app/main.py` 中注册：
   ```python
   app.add_middleware(NewMiddleware)
   ```

**证据**：`server/app/core/middleware.py:39-68`, `server/app/main.py:56`

---

## 10. 启动证据包（实际命令 + 输出）

### 10.1 启动命令

**实际命令**：
```powershell
cd e:\FiveTierProjectSystem\01-Inbox\dataAnalysisAgent\server
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**证据**：从对话历史中提取的启动命令

### 10.2 启动日志（实际输出）

**从服务日志提取**（`CheckCommandStatus` 输出）：
```
2026-07-13 08:09:04 | INFO     | app.main:30 | 启动数据分析智能体 API...
2026-07-13 08:09:04 | INFO     | app.main:32 | 数据库初始化完成
2026-07-13 08:09:04 | INFO     | uvicorn.error:84 | Started server process [10044]
2026-07-13 08:09:04 | INFO     | uvicorn.error:88 | Waiting for application startup.
2026-07-13 08:09:04 | INFO     | uvicorn.error:92 | Application startup complete.
2026-07-13 08:09:04 | INFO     | uvicorn.error:100 | Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

**请求日志示例**（从 `RequestLoggingMiddleware` 输出）：
```
[2026-07-13 08:09:05] GET /health 200 0.001s
[2026-07-13 08:09:06] GET /api/v1/projects/ 200 0.045s
[2026-07-13 08:09:07] GET /api/v1/projects/ 401 0.002s
```

### 10.3 健康检查验证

**实际请求**：
```bash
curl http://localhost:8000/health
```

**实际响应**：
```json
{
  "status": "ok",
  "service": "data-analysis-agent"
}
```

**证据**：`server/app/main.py:109-112`

### 10.4 认证验证（实际测试）

**无 token 访问**：
```bash
curl http://localhost:8000/api/v1/projects/
```
**响应**：`401 Unauthorized`  
**证据**：验证脚本输出

**无效 token 访问**：
```bash
curl -H "Authorization: Bearer invalid-token" http://localhost:8000/api/v1/projects/
```
**响应**：`401 Unauthorized`  
**证据**：验证脚本输出

**dev-token 访问**：
```bash
curl -H "Authorization: Bearer dev-token" http://localhost:8000/api/v1/projects/
```
**响应**：`200 OK`  
**证据**：验证脚本输出

### 10.5 CORS 验证（实际测试）

**预检请求**：
```bash
curl -X OPTIONS http://localhost:8000/api/v1/projects/ \
  -H "Origin: http://localhost:3000" \
  -H "Access-Control-Request-Method: GET" \
  -H "Access-Control-Request-Headers: Authorization"
```

**响应头**：
```
Access-Control-Allow-Origin: http://localhost:3000
Access-Control-Allow-Methods: GET, POST, PUT, PATCH, DELETE
Access-Control-Allow-Headers: Authorization, Content-Type
```

**证据**：验证脚本输出

---

## 附录：未明确事项清单

| 事项 | 说明 |
|-----|------|
| Python 精确版本 | 无 `.python-version` 或 `pyproject.toml` 文件，仅能推断 >=3.10 |
| `openai` 库使用位置 | 可能在 `services/llm/client.py`，但未详细读取该文件 |
| 数据归属校验中间件 | 当前在路由层手动过滤，未明确是否有统一中间件 |
| Alembic 迁移配置 | `requirements.txt` 包含 `alembic>=1.13.0`，但未检查 `alembic.ini` 和 `migrations/` 目录 |
| 生产环境部署配置 | Docker Compose（backend + frontend + db + nginx），已在 124.223.21.212 部署 |

---

## 11. 后续开发路线图（大阶段 → 小阶段拆解）

> **当前节点**：V1~V9 核心功能链路已闭环，生产环境已部署（2026-07-14），待接入真实登录与支付后正式开放注册。
> **拆解原则**：每个小阶段是一个可独立开发、独立验收、独立提交的任务单元，对应 [开发任务模板](./templates/开发任务模板.md)。

### 阶段一：MVP 上线准备（S1 系列）

> **目标**：从"可演示"到"可收费"，打通真实用户登录 → 付费 → 使用全链路。
> **阻塞性**：S1-1 和 S1-2 为上线硬阻塞，其余为建议项。

#### S1-1 微信登录接入（替换 dev-token）

| 维度 | 内容 |
|------|------|
| **目标** | 接入微信网页授权 OAuth2.0，替换 dev-token 调试登录，实现真实用户身份流转 |
| **后端改动** | `api/v1/auth.py` 新增 `/auth/wechat/callback` 端点：code → openid → upsert User → 签发 JWT；`core/security.py` 复用现有 JWT 生成逻辑 |
| **数据库** | User 表 `openid` 字段已有（`models/user.py:18`），无需改 schema |
| **环境变量** | 新增 `WECHAT_APP_ID` / `WECHAT_APP_SECRET` / `WECHAT_REDIRECT_URI` |
| **前端配合** | 登录页 `/login` 跳转微信授权链接；BFF `/api/auth/callback` 接收 code 转发后端 |
| **验收标准** | ① 微信扫码后回调拿到 JWT ② JWT 可访问所有需认证端点 ③ dev-token 仅 DEBUG 模式可用（已实现）④ 新用户首次登录自动创建 User 记录 |
| **依赖** | 微信公众平台账号（AppID + AppSecret） |
| **优先级** | **P0 阻塞上线** |

#### S1-2 微信支付集成（9.9 单次 / 19.9 月订阅）

| 维度 | 内容 |
|------|------|
| **目标** | 接入微信支付 Native API（PC 扫码），实现套餐购买 → 支付回调 → 自动激活套餐 |
| **后端改动** | 新增 `api/v1/payment.py` 路由模块：`POST /payment/create`（下单生成预支付二维码）+ `POST /payment/notify`（微信异步回调，验签后更新 User.plan + plan_expires_at）；新增 `services/wechat_pay.py` 封装微信支付 SDK |
| **数据库** | Order 表已有（`models/order.py`），字段：user_id / amount / plan_type / status / wx_transaction_id；可能需加 `expires_at` 字段记录套餐到期时间 |
| **环境变量** | 新增 `WECHAT_PAY_MCH_ID` / `WECHAT_PAY_API_KEY` / `WECHAT_PAY_NOTIFY_URL` |
| **权限联动** | 回调成功后更新 `User.plan`（free→single/subscription）+ `User.plan_expires_at`，现有 `require_paid_plan` 依赖（`dependencies.py:84-93`）自动生效 |
| **前端配合** | 定价页 `/pricing` → 支付弹窗（二维码）→ 轮询订单状态 → 支付成功跳转 |
| **验收标准** | ① 扫码支付后 User.plan 正确更新 ② 套餐过期后自动降级为 free ③ 回调验签防篡改 ④ Order 表记录完整交易流水 |
| **依赖** | 微信商户平台账号（MchID + API Key）+ S1-1 完成（需登录态） |
| **优先级** | **P0 阻塞上线** |

#### S1-3 数据导出后端对接（V8 完整实现）

| 维度 | 内容 |
|------|------|
| **目标** | 前端 `/projects/[id]/export` 页面已有 UI，需对接后端真实生成 Excel/CSV 下载 |
| **后端改动** | `api/v1/simulation.py` 新增 `GET /simulation/export-data/{project_id}` 端点：读取 Dataset → openpyxl 生成 Excel / csv 生成 CSV → 注入 simulated 元数据水印 → 返回 StreamingResponse |
| **水印要求** | 文件首行/元数据标注 `simulated` + 生成时间 + 项目名，符合宪法第 7 条 |
| **前端配合** | BFF `/api/simulation/[id]/export-data` 透传 blob 响应；DownloadButton 触发浏览器下载 |
| **验收标准** | ① 下载的 Excel/CSV 含 simulated 水印 ② 数据内容与 Dataset 一致 ③ 免费用户返回 403 |
| **依赖** | 项目已有 Dataset 数据（需先跑过数据生成） |
| **优先级** | P1（上线前建议完成） |

#### S1-4 端到端冒烟测试（生产环境）

| 维度 | 内容 |
|------|------|
| **目标** | 在生产环境（124.223.21.212）走通完整用户路径，确保无阻断 |
| **测试路径** | 登录(S1-1) → 新建项目 → 上传题目 → 体检 → 付费(S1-2) → 假设解析 → 数据生成 → 统计分析 → 报告生成 → Word/Excel 导出 → 数据导出(S1-3) |
| **验证脚本** | 在项目根（非 server/ 内，避免触发 reload）创建验证脚本，参考 `project_memory` 中的三层验证规范（后端 9 项 + BFF 6 项 + 浏览器 13 项） |
| **验收标准** | ① 全流程无 500 错误 ② LLM 调用正常（agnes-2.0-flash）③ 数据库读写一致 ④ 前端页面渲染正常 ⑤ 导出文件可下载且含水印 |
| **依赖** | S1-1 + S1-2 + S1-3 完成 |
| **优先级** | P1（上线前必须完成） |

#### S1-5 监控与告警

| 维度 | 内容 |
|------|------|
| **目标** | 基础错误监控 + 关键业务指标告警，确保上线后可感知问题 |
| **后端改动** | `core/logging.py` 已有文件日志（10MB 轮转），补充：LLM 调用失败率统计、支付回调异常告警、数据库连接池监控 |
| **部署层** | Docker logs 采集 + 可选接入告警通知（邮件/企业微信机器人） |
| **验收标准** | ① LLM 连续失败 3 次触发告警 ② 支付回调验签失败触发告警 ③ 日志可追溯全链路请求 |
| **依赖** | 无 |
| **优先级** | P2（可上线后补充） |

---

### 阶段二：V2 扩展（S2 系列）

> **目标**：上线后有付费用户验证后启动，围绕增长和品类扩展。
> **启动前提**：阶段一完成 + 至少有少量真实付费用户使用反馈。

#### S2-1 教程站（SEO 引流引擎）

| 维度 | 内容 |
|------|------|
| **目标** | `/tutorials` 路由扩展，首批 30~50 篇覆盖核心 SEO 关键词（信效度、KMO、Cronbach α、问卷设计等） |
| **技术方案** | Next.js 同框架扩展，LLM 生成初稿 + 人工校对，MDX 渲染 |
| **验收标准** | ① 教程页可被搜索引擎收录 ② 内链指向产品功能页 ③ 移动端可读 |
| **优先级** | P1（增长引擎） |

#### S2-2 问卷星链接解析

| 维度 | 内容 |
|------|------|
| **目标** | 支持用户粘贴问卷星链接，自动抓取题目结构（替代手动粘贴） |
| **后端改动** | 新增 `services/wenjuanxing_parser.py`，需评估反爬策略与鉴权 |
| **风险** | 反爬对抗、题目结构解析鲁棒性 |
| **验收标准** | ① 主流问卷星链接可解析出题干列表 ② 解析失败时降级为手动粘贴 ③ 不存储用户问卷星凭据 |
| **优先级** | P2 |

#### S2-3 PDF / PPT 报告导出

| 维度 | 内容 |
|------|------|
| **目标** | 当前仅 Word + Excel 导出，新增 PDF（便于分享）和 PPT（便于答辩展示）格式 |
| **后端改动** | `services/reporter.py` 扩展：PDF 用 reportlab 或 weasyprint；PPT 用 python-pptx |
| **验收标准** | ① 导出文件含 simulated 水印 ② 排版与 Word 版一致 ③ 答辩用 PPT 含图表截图 |
| **优先级** | P2 |

#### S2-4 题目修改建议自动改写（R5）

| 维度 | 内容 |
|------|------|
| **目标** | R4 诊断不达标时，不仅给修改建议，还自动给出改写后的题目文本 |
| **后端改动** | `services/diagnoser.py` 扩展：LLM 根据诊断 issue 生成改写建议；新增 `schemas/report.py` 字段 `rewrite_suggestions` |
| **验收标准** | ① 改写建议针对具体诊断问题 ② 用户可一键采纳替换原题 ③ 采纳后可重新体检 |
| **优先级** | P2 |

#### S2-5 进阶统计（硕博人群扩展）

| 维度 | 内容 |
|------|------|
| **目标** | 新增 SEM/CFA/中介调节分析，从本科毕设扩到硕博人群 |
| **后端改动** | `services/stats.py` 扩展：引入 semopy（SEM）、factor_analyzer（CFA）、pingouin（中介调节） |
| **数据库** | 可能需扩展 Report 表或新增 AdvancedStatResult 表 |
| **验收标准** | ① 模型拟合指标正确（CFI/TLI/RMSEA）② 中介效应 bootstrap 置信区间 ③ 结果可导出 |
| **优先级** | P3（后期） |

#### S2-6 报告文字润色（R6）

| 维度 | 内容 |
|------|------|
| **目标** | LLM 润色诊断结论和报告文字，使其更学术化、更易读 |
| **后端改动** | `services/reporter.py` 新增润色环节：统计结果 → LLM 生成学术化解读段落 |
| **验收标准** | ① 润色后文字符合学术规范 ② 不篡改统计数值 ③ 用户可对比原版和润色版 |
| **优先级** | P3（后期） |

#### S2-7 B 站视频引流

| 维度 | 内容 |
|------|------|
| **目标** | 10~20 个短视频教程，B 站引流到产品 |
| **分工** | 非开发任务，内容制作 + 视频剪辑 |
| **优先级** | P3（运营阶段） |

---

### 路线图总览

```
当前节点（2026-07-15）
    │
    ├─ S1-1 微信登录 ──────┐
    ├─ S1-2 微信支付 ──────┤（可并行：S1-3 数据导出对接）
    │                       │
    ├─ S1-4 冒烟测试 ◄──────┘（依赖 S1-1~S1-3）
    │
    ├─ S1-5 监控告警（可并行/上线后补）
    │
    ▼
【MVP 正式上线】
    │
    ├─ S2-1 教程站（SEO）
    ├─ S2-2 问卷星解析
    ├─ S2-3 PDF/PPT 导出
    ├─ S2-4 R5 题目改写
    ├─ S2-5 进阶统计（硕博）
    ├─ S2-6 R6 报告润色
    └─ S2-7 B站视频引流
    │
    ▼
【V2 增长期】
```

---

**文档结束**
